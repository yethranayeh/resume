{
	"name": "@aktasalper/resume",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc -b && vite build",
		"preview": "vite preview",
		"deploy": "git subtree push --prefix dist origin gh-pages",
		"pipeline": "npm run build && git add -A && git commit -m 'build files' && git push && npm run deploy"
	},
	"dependencies": {
		"i18next": "^25.4.1",
		"lodash": "^4.17.21",
		"preact": "^10.24.1",
		"react-i18next": "^15.4.0",
		"react-router-dom": "^6.27.0"
	},
	"devDependencies": {
		"@preact/preset-vite": "^2.9.1",
		"sass": "^1.79.5",
		"typescript": "^5.5.3",
		"vite": "^5.4.8"
	}
}
